# Two independent walkthroughs in one doc

```ts
const alpha: number = 1
```

```ts
const useAlpha: number = alpha + 1
```

A separate, unrelated example:

```ts
const beta: number = 2
```

```ts
const useBeta: number = beta + 1
```
